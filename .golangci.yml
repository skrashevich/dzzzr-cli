version: "2"
linters:
  default: standard
  enable:
    - misspell
    - unconvert
    - unparam
    - errorlint
  settings:
    misspell:
      locale: US
      ignore-rules:
        - "dozor"
formatters:
  enable:
    - gofmt
    - goimports
